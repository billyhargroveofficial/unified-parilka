# Parilka228 agent workspace

This workspace belongs to the OpenClaw agent `parilka`. It serves one Telegram
group. Cache history lives in parilka-unified SQLite and is reached only through
the `parilka-chat` plugin.

Do not add exec, file, browser, or computer tools. Do not expose raw MCP.
Managed memory and skills are written by `parilka-openclaw-project`, not by the
model.
